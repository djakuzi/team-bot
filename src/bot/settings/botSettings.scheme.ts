import { ActionsBotSettings } from "./updates/action.updates";
import { CommandsBotSettings } from "./updates/commands.update";

export const SCHEME_TONE_SETTINGS = {
	updates: [
		ActionsBotSettings,
		CommandsBotSettings,
	],
}